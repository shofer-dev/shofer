# Set TELEMETRY_ENABLED=true to enable telemetry (disabled by default).
# When disabled (the default), no data is sent to PostHog or Shofer Cloud —
# no server-side infrastructure is required.
# TELEMETRY_ENABLED=true

POSTHOG_API_KEY=key-goes-here

# Shofer Cloud / Local Development
CLERK_BASE_URL=https://epic-chamois-85.clerk.accounts.dev
SHOFER_API_URL=http://localhost:3000
SHOFER_PROVIDER_URL=http://localhost:8080/proxy/v1
