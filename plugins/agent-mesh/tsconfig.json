{
	"//": "Standalone tsconfig for the agent-mesh plugin (transpiled at runtime by the plugin loader; outside every extension/package tsc root). Lets a dev/CI typecheck it against @shofer/types + nats in isolation.",
	"extends": "@shofer/config-typescript/base.json",
	"compilerOptions": {
		"noEmit": true,
		"types": ["node"],
		"baseUrl": ".",
		"paths": { "@shofer/types": ["../../packages/types/src/index.ts"] }
	},
	"include": ["*.ts"],
	"exclude": ["node_modules"]
}
